server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Optional: Set headers for SPA
    add_header Cache-Control "no-store";
    add_header X-Content-Type-Options nosniff;
}
